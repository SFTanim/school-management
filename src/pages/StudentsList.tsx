import PageTitle from "../components/PageTitle";

const StudentsList = () => {
    return (
        <div>
            <PageTitle heading="students data"/>
        </div>
    );
};

export default StudentsList